<Page x:Class="Chord_Finder.Pages.AllNotesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="All Notes Page" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
    
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="0">
            This is the All Notes Page!
        </TextBlock>
        
        <Viewbox Grid.Row="1" Margin="15">
            <Canvas x:Name="FretboardCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="1200" Height="350" />
        </Viewbox>
        
    </Grid>
</Page>
